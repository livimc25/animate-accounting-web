
<NrgButton @class="move-over" @action={{this.toggleShowHello}}> 
  <AnimatedContainer>
      {{#animated-if this.showHello rules=this.rules}}
        <div class="answer">
          {{@answer}}
        </div>
      {{else}}
        <div class="question">
          {{@question}}
        </div>
      {{/animated-if}}
    </AnimatedContainer>
</NrgButton>

{{yield}}